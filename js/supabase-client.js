// =============================================================
// Supabase 클라이언트 초기화 (supabase-client.js)
// -------------------------------------------------------------
// CDN으로 불러온 supabase-js 라이브러리를 사용해
// 우리 프로젝트에 연결하는 "창고 열쇠(sb)"를 만듭니다.
// 이후 모든 페이지에서 sb.xxx() 로 데이터/로그인을 다룹니다.
// =============================================================

// CDN 스크립트가 전역 변수 `supabase` 를 만들어 줍니다.
const sb = supabase.createClient(CONFIG.SUPABASE_URL, CONFIG.SUPABASE_ANON_KEY);
